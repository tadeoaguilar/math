import datasets
from _typeshed import Incomplete
from dataclasses import dataclass
from datasets.table import table_cast as table_cast
from datasets.utils.file_utils import readline as readline

logger: Incomplete

@dataclass
class JsonConfig(datasets.BuilderConfig):
    """BuilderConfig for JSON."""
    features: datasets.Features | None = ...
    encoding: str = ...
    encoding_errors: str | None = ...
    field: str | None = ...
    use_threads: bool = ...
    block_size: int | None = ...
    chunksize: int = ...
    newlines_in_values: bool | None = ...
    def __init__(self, name, version, data_dir, data_files, description, features, encoding, encoding_errors, field, use_threads, block_size, chunksize, newlines_in_values) -> None: ...

class Json(datasets.ArrowBasedBuilder):
    BUILDER_CONFIG_CLASS = JsonConfig
