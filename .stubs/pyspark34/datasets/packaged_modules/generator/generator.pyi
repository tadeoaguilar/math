import datasets
from dataclasses import dataclass
from typing import Callable

@dataclass
class GeneratorConfig(datasets.BuilderConfig):
    generator: Callable | None = ...
    gen_kwargs: dict | None = ...
    features: datasets.Features | None = ...
    def __post_init__(self) -> None: ...
    def __init__(self, name, version, data_dir, data_files, description, generator, gen_kwargs, features) -> None: ...

class Generator(datasets.GeneratorBasedBuilder):
    BUILDER_CONFIG_CLASS = GeneratorConfig
