import threading
from ..registry import CollectorRegistry as CollectorRegistry, REGISTRY as REGISTRY
from typing import Callable, Tuple

class _RegularPush(threading.Thread):
    def __init__(self, pusher, interval, prefix) -> None: ...
    def run(self) -> None: ...

class GraphiteBridge:
    def __init__(self, address: Tuple[str, int], registry: CollectorRegistry = ..., timeout_seconds: float = 30, _timer: Callable[[], float] = ..., tags: bool = False) -> None: ...
    def push(self, prefix: str = '') -> None: ...
    def start(self, interval: float = 60.0, prefix: str = '') -> None: ...
